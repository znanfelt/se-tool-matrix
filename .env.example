# Cloudflare environment variables
# Copy this file to .env for local development
# NEVER commit actual API keys to version control!

# Note: Cloudflare AI is used automatically with Cloudflare Workers AI
# No API key needed - AI is available via env.AI in Cloudflare Functions

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. No additional setup needed for Cloudflare AI
# 3. The .env file is in .gitignore and will NOT be committed
# 4. Cloudflare AI is automatically available in production via env.AI
